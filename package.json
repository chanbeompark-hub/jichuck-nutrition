{
  "name": "nutrition-guide",
  "version": "1.0.0",
  "private": true,
  "description": "피트니스 박부장 · 박찬범 트레이너 — 회원용 식단 칼로리 계산기",
  "scripts": {
    "build": "node -e \"console.log('빌드 단계 없음 — public/ 을 그대로 배포합니다')\"",
    "dev": "npx wrangler pages dev --port 8789",
    "deploy": "npx wrangler pages deploy",
    "tail": "npx wrangler pages deployment tail --project-name jichuck-nutrition"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
